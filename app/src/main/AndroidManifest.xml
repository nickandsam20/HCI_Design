<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hci_demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hci_demo"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".camara2"></activity>
        <activity android:name=".camera" />
        <activity android:name=".new_menu" />
        <activity android:name=".setting_gesture_passwd" />
        <activity android:name=".setting_backup_passwd" />
        <activity android:name=".setting_passwd" />
        <activity android:name=".setting_difficulity" />
        <activity android:name=".setting_resttime" />
        <activity android:name=".setting_mouse_choose" />
        <activity android:name=".menu_horizontal_scroll" />
        <activity android:name=".menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera2.full"/>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>